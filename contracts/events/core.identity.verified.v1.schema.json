{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.wasla/events/core.identity.verified.v1.schema.json",
  "title": "core.identity.verified v1",
  "description": "Emitted by CORE when a new identity is established and verified on a channel. Not emitted for repeat resolutions of an existing channel account. Consumers: MOVE (may create its own driver/rider profile keyed by identity_id), MARKET (may create its own customer profile keyed by identity_id). Neither consumer receives identity attributes beyond the ids below.",
  "type": "object",
  "required": ["identity_id", "principal_id", "channel_type", "verified_at"],
  "additionalProperties": false,
  "properties": {
    "identity_id": { "type": "string", "format": "uuid" },
    "principal_id": { "type": "string", "format": "uuid" },
    "channel_type": {
      "type": "string",
      "enum": ["telegram", "phone", "email", "web", "partner_api"]
    },
    "verified_at": { "type": "string", "format": "date-time" }
  },
  "examples": [
    {
      "identity_id": "3f1c2b64-8f2e-4f52-9d1a-1c6bb9f9d001",
      "principal_id": "8a2c6d11-2f77-4a0e-9a44-b1b7f3c2e002",
      "channel_type": "telegram",
      "verified_at": "2026-01-01T00:00:00.000Z"
    }
  ]
}
