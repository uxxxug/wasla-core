{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.wasla/events/core.fulfillment.cancelled.v1.schema.json",
  "title": "core.fulfillment.cancelled v1",
  "description": "Published by CORE when a fulfillment is cancelled before execution closes. MARKET closes the commercial order; MOVE stops any operational job it created.",
  "type": "object",
  "additionalProperties": false,
  "required": ["fulfillment_id", "order_reference", "reason", "cancelled_at"],
  "properties": {
    "fulfillment_id": { "type": "string", "format": "uuid" },
    "order_reference": { "type": "string", "minLength": 1 },
    "reason": { "type": "string", "minLength": 1 },
    "cancelled_at": { "type": "string", "format": "date-time" },
    "settlement_state": {
      "type": "string",
      "enum": ["none", "held", "captured", "partially_captured", "released", "unsettled"],
      "description": "State of the CORE money hold at closure. `released` means the hold was returned and no money moved; `partially_captured` means part of the consented amount had already moved and only the remainder was returned, so the payer has paid for work that was cancelled; `unsettled` means CORE could not bring the hold to a terminal state. The last two need reconciliation. Optional and additive; consumers may ignore it, and a consumer that reads it must treat an unrecognised value as \"do not assume\" rather than as `released`."
    },
    "captured_minor": {
      "type": "integer",
      "minimum": 0,
      "description": "The amount of the hold, in minor units, that CORE observed as already moved at cancellation. Present only when CORE was given the figure by its money module; omitted when it was not observed, which is deliberately different from zero. Never treat an absent value as zero."
    },
    "financial_decision_required": {
      "type": "boolean",
      "description": "True when money had already moved for work that was then cancelled, and CORE has not been told what should happen to it. It is a statement about CORE's knowledge, not a policy: whether that amount is refunded, retained against work performed, or charged as a cancellation fee is undecided outside CORE (blocker B-20). While it is true a consumer MUST NOT tell the payer they were refunded, MUST NOT invoice the amount as earned, and MUST NOT treat the order as financially settled. A cancellation is not a refund. Absent or false means no such amount is pending."
    }
  },
  "examples": [
    {
      "fulfillment_id": "e53bd31b-06e4-4609-a109-fcfe6848376d",
      "order_reference": "order-1",
      "reason": "customer_cancelled",
      "cancelled_at": "2026-09-11T03:42:00.000Z",
      "settlement_state": "released",
      "financial_decision_required": false
    },
    {
      "fulfillment_id": "e53bd31b-06e4-4609-a109-fcfe6848376d",
      "order_reference": "order-1",
      "reason": "customer_cancelled",
      "cancelled_at": "2026-09-11T03:42:00.000Z",
      "settlement_state": "partially_captured",
      "captured_minor": 2500,
      "financial_decision_required": true
    }
  ]
}
