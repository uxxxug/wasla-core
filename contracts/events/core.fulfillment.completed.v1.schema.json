{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.wasla/events/core.fulfillment.completed.v1.schema.json",
  "title": "core.fulfillment.completed v1",
  "description": "Published by CORE after MOVE reports the final execution outcome.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "fulfillment_id",
    "order_reference",
    "outcome",
    "completed_at"
  ],
  "properties": {
    "fulfillment_id": {
      "type": "string",
      "format": "uuid"
    },
    "order_reference": {
      "type": "string",
      "minLength": 1
    },
    "outcome": {
      "type": "string",
      "enum": [
        "completed",
        "failed"
      ]
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "reason": {
      "type": [
        "string",
        "null"
      ],
      "description": "Closure reason when the outcome is failed. Values produced by CORE include move_execution_failed, payment_settlement_failed:*, payment_hold_not_found, payment_hold_already_captured, payment_hold_not_authorized and payment_hold_expired."
    },
    "settlement_state": {
      "type": "string",
      "enum": [
        "none",
        "held",
        "captured",
        "partially_captured",
        "released",
        "unsettled"
      ],
      "description": "State of the CORE money hold at closure. A completed outcome is captured, partially_captured or none; a failed outcome is released, partially_captured, none or unsettled. `released` means no money moved at all, `partially_captured` means part of the consented amount moved and the remainder was released, and `unsettled` means CORE could not bring the hold to a terminal state. Optional and additive; consumers may ignore it, and a consumer that reads it must treat an unrecognised value as \"do not assume\" rather than as `released`."
    }
  },
  "examples": [
    {
      "fulfillment_id": "e53bd31b-06e4-4609-a109-fcfe6848376d",
      "order_reference": "order-2",
      "outcome": "completed",
      "completed_at": "2026-09-11T03:45:00.000Z",
      "settlement_state": "captured"
    }
  ]
}
